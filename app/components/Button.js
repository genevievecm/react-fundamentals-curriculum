import React, { Component } from 'react';

const Button = (props) => {
    return (
        <button
            className="btn"
            type="submit">
            Get Weather
        </button>
    )
}

export default Button;